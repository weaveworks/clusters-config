apiVersion: capi.weave.works/v1alpha2
kind: SecretSync
metadata:
  name: awssm-secret
  namespace: flux-system
spec:
  clusterSelector:
    matchLabels:
      weave.works/secret: sync
  secretRef:
    name: awssm-secret
  targetNamespace: flux-system